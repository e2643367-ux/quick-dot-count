# DOT / SNAP

一瞬だけ表示される点の数を直感で当てる、エンドレス型のブラウザゲームです。点の数と位置は各ラウンドでランダムに変化し、連続正解によるコンボ倍率、ローカルベストスコア、残り時間表示、効果音を備えています。

## GitHub Pages

`main` ブランチへのプッシュで、GitHub Actionsが静的サイトをビルドし、GitHub Pagesへ公開します。公開用ビルドは `pnpm build:pages` で実行できます。GitHub Pages用のロゴ・背景・演出素材は `client/public/` に同梱します。
