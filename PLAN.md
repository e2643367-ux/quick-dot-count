# Game Plan: DOT / SNAP

## Risk Tasks

### 1. 一瞬表示されるドットの安全な配置
- **Why isolated:** 近接・重なり・端への偏りがあると、プレイヤーが数を判断できないか、見た目の数と実数が一致しなくなる。
- **Approach:** 候補点をプレイフィールド内の安全余白に乱数配置し、既存の各点との最小距離を満たすものだけを採用する。所定回数で配置できない場合は再試行する。
- **Verify:** 1〜20個の各ケースで、点が端からはみ出さず、隣接点が重ならず、描画個数が問題の正解数と一致する。

### 2. 表示・回答・判定のラウンド状態遷移
- **Why isolated:** 短時間の自動切替中に入力を受けると、不正な回答、二重採点、または次ラウンドへの誤遷移が起こり得る。
- **Approach:** `intro → reveal → answer → result → complete` を明示状態として保持し、`answer` のときだけ入力を許可する。状態ごとにタイマーを一つだけ持ち、画面離脱時に必ず解放する。
- **Verify:** 表示中・判定中は数字キーとクリックが採点されず、回答は1ラウンドにつき1回だけ受理される。回答後は正誤と誤差が正しく出て、次ラウンドで新しい問題だけが表示される。

## Main Build

- 終了しない点数当てゲーム。プレイヤーは一瞬現れる点を見て、1〜20個なら数字キーまたは画面上の数字キーで、21個以上なら数値入力で回答する。5ラウンドごとにレベルと点数の範囲は無制限に増え、表示時間は最短180msまで短くなる。
- 点の数と位置は、各ラウンドでブラウザの暗号学的乱数を使って生成する。新規セッションやリセット後に過去の問題列を再現しない。
- 表示中は残り時間を明示するプログレスバーを表示する。連続正解は3問ごとに0.5倍ずつ倍率が高まり、正答時の加点として示す。正解、不正解、レベルアップはそれぞれ異なる短い効果音でフィードバックする。
- プレイ中は安全に一時停止・再開でき、リセットでは現在セッションだけを初期状態に戻す。ベストスコアはブラウザ内に保存し、リセット後も表示を維持する。レベルが上がる場面では、次の信号の前に短い専用演出を表示する。
- 正解は100点に連続正解ボーナスを加算し、誤答は誤差と正解数を示す。最終画面では正答数・得点・最高連続正解を表示し、再挑戦できる。
- 左の測定レール、右の観測フィールド、モバイル用の再配置を採用する。`?demo` では決定された問題列と自動回答で、実際のゲーム進行を視覚確認できる。
- **Assets needed:**
  - 1920×1080のインクブルー背景テクスチャ
  - 512×512の4点クラスターロゴ
  - 512×512の正解用粒子バースト
  - 16:9のゲーム画面リファレンス
- **Verify:**
  - マウスとキーボードの入力で同じ回答処理が動く。
  - 点数、連続正解、ラウンド数、正誤・誤差が常に整合する。
  - UIはデスクトップとモバイルで重ならず、文字と数値を読み取れる。
  - 背景・ロゴ・正解エフェクトが読み込まれ、プレースホルダーがない。
  - 実行中にブラウザコンソールエラーがない。
  - リファレンスと比べ、濃紺・シトラスの配色、非対称レイアウト、広い観測フィールド、抑制された情報密度が保たれている。
  - **プレゼンテーション確認:** `?demo` で表示から回答、判定までの実ゲーム状態が見える。
